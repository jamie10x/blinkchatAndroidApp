package com.jamie.blinkchat.core.mvi

/**
 * A marker interface for all UI states in the MVI architecture.
 * Represents the data needed to render a specific screen at a given point in time.
 * Implementations should be immutable data classes.
 */
interface UiState